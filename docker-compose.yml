version: '3'

services:
  db:
    image: mariadb
    container_name: db
    restart: always
    hostname: db_host
    environment:
      MARIADB_ROOT_PASSWORD: fHKSDbf231dsfsnsdfs
      MARIADB_DATABASE: jshop
      MARIADB_USER: user
      MARIADB_PASSWORD: letsCELEBRATEandSuckSOMEDICK
    networks:
      - default

  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    container_name: phpmyadmin
    restart: always
    ports:
      - 8080:80
    environment:
      PMA_USER: user
      PMA_PASSWORD: letsCELEBRATEandSuckSOMEDICK
      PMA_HOST: db_host
      PMA_PORT: 3306
    networks:
      - default

  api:
    container_name: api
    restart: always
    hostname: api_host
    build:
      context: api
      dockerfile: api.Dockerfile
    environment:
      JWT_SECRET: "dshkgdikjfgb972134523"
      JWT_EXPIRESIN: "3600s"
      DATABASE_USERNAME: "user"
      DATABASE_PASSWORD: "letsCELEBRATEandSuckSOMEDICK"
      DATABASE_DATABASE: "jshop"
      DATABASE_HOST: db_host
    ports:
      - "3000:3000"
    networks:
      - default

  frontend:
    container_name: frontend
    restart: always
    hostname: frontend_host
    build:
      context: frontend
      dockerfile: frontend.Dockerfile
    environment:
      VUE_APP_GATEMAY_ADDRESS: "api.kontinuum.su"
    ports:
      - "80:80"
    networks:
      - default

networks:
  default:
    external:
      name: microservices_network
